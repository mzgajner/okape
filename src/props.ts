// Common route properties for reuse in components

export const municipality = {
  type: String,
  required: true,
};

export const street = {
  type: String,
  required: true,
};

export const buildingType = {
  type: String,
  required: true,
};

export const pickupDays = {
  type: String,
  required: true,
};
